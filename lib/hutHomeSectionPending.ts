/** Při navigaci na `/` z jiné routy — AppShell po mountu přepne sekci (viz HutShell). */
export const HUT_PENDING_HOME_SECTION_KEY = "hut:pendingHomeSection";

export type HutPendingHomeSection = "inventar" | "optimalizator";
